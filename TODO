Major things
============
    * reimplement in C or Haskell
    * make a comp function that compares two or more fasta files
    * handle fasta indexing (this requires input from named files, not STDIN)

Minor things
============
    * deconvolute the coloring scheme
    * add color subcommand (color sequence by letter, optionally amino acid class)
    * add filename handling to grep (-l, -L, -h (as -s) options).
